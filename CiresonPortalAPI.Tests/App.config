<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <!-- URL to the Cireson Portal, without trailing slash -->
    <add key="PortalUrl" value=""/>
    
    <!-- User name to log in with -->
    <add key="UserName" value=""/>
    
    <!-- Password to log in with -->
    <add key="Password" value=""/>
    
    <!-- Domain of the UserName -->
    <add key="Domain" value=""/>
    
    <!-- Used to test user filter functionality. Specify a filter that identifies a single user with a manager. -->
    <add key="UserFilter" value=""/>
  </appSettings>
<startup><supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5"/></startup></configuration>
